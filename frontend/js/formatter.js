export class Formatter {
    Render(data, parentElement) {
        // базова реалізація або викидування помилки, якщо це повинен бути абстрактний метод
        throw new Error("render method must be implemented by subclass");
    }
}
